# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- Node ---
# 不带尾斜杠：跨 worktree 开发时 node_modules 常常是个软链，`node_modules/`
# 只匹配目录，会把软链漏进提交
node_modules
dist/
*.tsbuildinfo
.vite/

# --- Env & secrets ---
.env
.env.local
.env.*.local
*.pem
*.key

# --- Data / runtime ---
data/
# golden transcripts are committed test fixtures, not runtime data
!src/backend/tests/golden/data/
logs/
*.sqlite3
*.db

# --- OS / editor ---
.DS_Store
.idea/
.vscode/
*.swp

# --- Internal working docs (kept locally, not tracked) ---
docs-dev/

# --- 本地 AI 助手配置（不入库） ---
CLAUDE.md
.claude/

# setuptools build output (pip install . in a mounted container leaves this behind)
src/backend/build/

# electron-builder output (src/desktop/dist is covered by the global dist/ rule)
src/desktop/release/

# desktop packaging staging: downloaded uv binaries + copied backend sources
# (regenerated by `pnpm --dir src/desktop run stage:resources`)
src/desktop/.cache/
src/desktop/resources/

# vendored packages: declaration output is built by `pnpm run vendor:types`
vendor/deepseek-cordis/*/lib/
vendor/deepseek-cordis/*/tsconfig.tsbuildinfo

# Codex account state must stay outside source and build contexts.
.codex/
**/.codex/
auth.json
**/auth.json
